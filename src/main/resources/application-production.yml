server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=require
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1

  jpa:
    show-sql: false

logging:
  level:
    com.lottery: WARN
    org.flywaydb: WARN
    org.hibernate: WARN
